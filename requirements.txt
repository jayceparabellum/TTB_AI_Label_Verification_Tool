fastapi==0.135.4
uvicorn[standard]==0.34.0
jinja2==3.1.6
python-multipart==0.0.31
pillow==12.2.0
pytesseract==0.3.13
rapidfuzz==3.11.0
opencv-python-headless==4.11.0.86
numpy==2.2.1

# --- Layer 2: conversational agent (Phase A) ---
langgraph==1.2.5
langgraph-checkpoint-sqlite==3.1.0
langchain-core==1.4.7
langchain-ollama==1.1.0
# --- Layer 3: local RAG knowledge layer (Phase C) ---
rank-bm25==0.2.2
# Dense embeddings (BGE-small via sentence-transformers + chromadb) are host-deferred:
# retrieval is BM25-only and fully offline until the host is provisioned. Enable with:
#   sentence-transformers, chromadb   (see rag/retrieve.py DenseBackend)
