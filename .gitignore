__pycache__/
*.pyc
.venv/
venv/
.env
.env.*
!.env.example
*.egg-info/
.pytest_cache/
.ruff_cache/
dist/
build/
.DS_Store
# Synthetic degraded images are regenerated by run_eval.py — don't track them,
# but DO track real photo fixtures (they can't be regenerated).
eval/images/*
!eval/images/real/
.claude/settings.local.json

# Local agent persistence (checkpoints, audit, vector store) — never committed
.agent_data/

# Local CodeGraph index — per-developer, regenerated locally
.codegraph/
